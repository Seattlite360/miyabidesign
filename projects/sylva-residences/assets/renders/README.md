# Renders — placeholder

`sylva-render-01.png` and `sylva-render-02.png` belong here (the two AI-generated mood renders
referenced throughout the brief). They weren't retrievable as files in the session that built
the Illustrator kit in `../../illustrator/` — only the three markdown briefs came through as
actual uploads.

Drop the real files in here with those exact names, then place them onto a working artboard
for the art-direction pass described in `../../sylva-logomark-brief.md` (§5).

Both are AI-generated — any use in front of John needs the same AI-disclosure label used
elsewhere in the pitch.
